# Empire OS Handoff — 2026-08-17

## Session Summary

Two parallel agent investigations (architect + planner, 301s + 307s each) identified:
1. **Book Factory MVP built** (trends, metadata, factory orchestrator, 34 tests passing)
2. **Free video pipeline plan exists** but was never implemented
3. **Critical blockers** preventing real book generation

---

## IMMEDIATE PRIORITY: Build the Free Video Pipeline

**Location:** `CLAUDE_CODE_FREE_PROVIDERS_INTEGRATION.md` (exists, ready to implement)

### Three Adapters to Build (Total: ~2 hours)

1. **HyperFrames** (30 min)
   - Local HTML→MP4 renderer (infinite free, no API cost)
   - Renders Ken Burns + music sequences locally
   - **Payoff:** Full GG episodes render for FREE

2. **WaveSpeed Desktop** (20 min)
   - Local image generation + 4K upscaling
   - 12 built-in tools, no API key needed
   - **Payoff:** LO/IL image gen free, no Higgsfield credits

3. **Google Veo 3.1** (20 min)
   - Official Google API (10 free videos/month per account)
   - Hero shots only (quota limited), fallback chain to HyperFrames
   - **Payoff:** Peak moments rendered by Google for free

### Expected Impact
- **Before:** 320 Higgsfield credits per full render cycle
- **After:** ~80 credits (75% savings)
- **Per channel:** GG $0 (Wikimedia + HyperFrames) | LO/IL $20-30 (WaveSpeed gen)

### Implementation Checklist (from the doc)
```
Phase 1: HyperFrames adapter + ROUTING_TABLE entry
Phase 2: WaveSpeed adapter (after disk space check — needs ~2GB for models)
Phase 3: Veo 3.1 adapter + quota tracking
Phase 4: Integration test (verify providers picked before Higgsfield)
Phase 5: Commit + push
```

**This is what makes the pipeline actually free.**

---

## Book Factory Status (Secondary Priority)

### What's Built
- ✅ TrendScanner (8 evergreen niches, round-robin, 90-day cooldown)
- ✅ BookMetadata (ISBN generation, BISAC categories, niche pricing)
- ✅ BookFactory orchestrator (dry-run default, SQLite ledger)
- ✅ 34 tests passing
- ✅ CLI: scan / run-cycle / status / publish
- ✅ Committed to `feature/storyforge2-2026-08-14`

### Critical Blockers (Phase 0 Must Fix First)
1. **`storyforge2/pipeline.py` broken on import**
   - Missing: `PipelineState`, `StageStatus` classes
   - Fix: Rewrite against real `StateStore` API
   - Impact: Blocks all manuscript generation

2. **No Claude API wired**
   - Missing: `ANTHROPIC_API_KEY` in `.env`
   - Missing: Real implementation in `ai_router/adapters/claude_adapter.py` (currently a stub)
   - Workaround: Use Gemini (already in `.env`, already wired)
   - Impact: Blocks Phase 2 (20k-40k word manuscripts)

3. **Draft2Digital status contradicted**
   - Registry says DIRECT_API; CLAUDE.md says no API exists
   - Never tested against live endpoint
   - Action: Verify or demote status before Phase 4

4. **KDP policy vs code mismatch**
   - Code has Playwright browser automation (violates policy)
   - Fix: Book Factory produces manual upload package only

### Recommended Build Order
1. Phase 0 (1 day): Fix pipeline imports, add trend/niche to STAGES
2. Phase 1 (2-3 days): Trend scanning with one source (curated seed list)
3. Phase 2 (5+ days): Manuscript generation (50 chapters, resume-on-crash)
4. Phase 3 (2-3 days): Cover generation (Pollinations/DALL-E)
5. Phase 4 (decision-gated): Multi-platform publishing

**BUT:** Only do this AFTER the free video pipeline is working. Book Factory is infrastructure; the free pipeline is money-generating.

---

## What's Actually Generating Revenue vs What's Not

### Live (Making Money)
- YouTube channels: GG, LO, IL, EOE (monetized, but credit: checking Analytics needed)
- eBay inventory sync: Live (~10k cards), selling (but: checking seller account for numbers)
- Supabase + website infrastructure: All in place

### Built But Not Live
- Boss Listers (app built, not yet deployed to Vercel)
- Book Factory (code done, blockers prevent real books)
- Free video pipeline (plan written, adapters not built)

### Status Unknown
- YouTube revenue (need to check Analytics)
- eBay sales numbers (need to check seller account)
- Storefront conversions (need revenue tracking dashboard)

**Bottom line:** You have infrastructure but no visibility into whether it's working. Phase 5 of Book Factory (revenue tracking) doesn't exist yet.

---

## Immediate Next Steps

### Option A (Recommended): Build Free Video Pipeline Now
1. Read `CLAUDE_CODE_FREE_PROVIDERS_INTEGRATION.md` completely
2. Implement Phase 1 (HyperFrames) — 30 min
3. Implement Phase 2 (WaveSpeed) if disk space allows (~2GB needed)
4. Implement Phase 3 (Veo 3.1) — 20 min
5. Test on GG_EP001 (verify HyperFrames renders locally, zero Higgsfield credits)
6. Commit + push
7. **Result:** 75% cost reduction for video pipeline

### Option B: Debug Book Factory Phase 0
1. Fix `storyforge2/pipeline.py` imports
2. Add `trend` + `niche` to `state.py` STAGES/APPROVAL_GATES
3. Wire real Claude API or use Gemini
4. Run Phase 1-2 end-to-end with mock provider (free)
5. **Result:** Validated book pipeline infrastructure (no revenue yet)

**Recommendation:** Do Option A first (2 hours → immediate savings). Book Factory is infrastructure that requires decisions (ANTHROPIC_API_KEY, D2D verification, KDP policy) and doesn't generate revenue until Phase 4+.

---

## Files & Locations

### Ready to Build
- `CLAUDE_CODE_FREE_PROVIDERS_INTEGRATION.md` — Complete implementation guide for free adapters
- `storyforge2/books/` — Book Factory MVP (trends, metadata, factory, 34 tests)
- `ai_router/router.py` — Where to wire the three new adapters
- `empire_render.py` — Where to call router instead of Higgsfield directly

### Blockers to Fix
- `storyforge2/pipeline.py` — Fix imports
- `storyforge2/state.py` — Add trend/niche to STAGES/APPROVAL_GATES
- `ai_router/adapters/claude_adapter.py` — Implement real Claude API or stub
- `.env` — Add ANTHROPIC_API_KEY (if going Claude route)

### Recently Committed
- `feat(storyforge2): build Book Factory foundation` (d7021cc)
- `docs: update CLAUDE.md with Book Factory architecture` (34ad693)

---

## Key Decisions Needed from Josh

1. **Free video pipeline:** Build it now? (Recommended: YES)
2. **Book Factory Phase 2:** Use Gemini (free, already wired) or add Claude API?
3. **Revenue tracking:** Want a dashboard to see actual YouTube/eBay numbers?
4. **Priority:** Video cost reduction (immediate ROI) vs book infrastructure (long-term)?

---

## Cost Impact

**If you build free video pipeline:**
- Current monthly: Higgsfield 300+ credits = ~$300+
- After: ~80 credits = ~$80
- **Savings: ~$220/month, 75% reduction**

**Book Factory:**
- One real book generation: ~50 Claude API calls (1.5M tokens) = ~$2-3
- Viable after Phase 4 (multi-platform publishing)
- ROI depends on book sales price

---

**That's the full state. What's next?**
