"""
council/roles — 10 specialized council members (Director → Performance Analyst).

Additive to council/bots/: these live in their own directory so the existing
council.py bot discovery (which scans council/bots/) is untouched. Run them
via council/roles/run_roles.py or import individually.
"""
